version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: couponify-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - couponify-network

  # Backend service (optional - for production)
  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: couponify-backend
  #   restart: unless-stopped
  #   ports:
  #     - "5000:5000"
  #   environment:
  #     - NODE_ENV=production
  #     - REDIS_HOST=redis
  #   depends_on:
  #     - redis
  #   networks:
  #     - couponify-network

volumes:
  redis_data:

networks:
  couponify-network:
    driver: bridge
