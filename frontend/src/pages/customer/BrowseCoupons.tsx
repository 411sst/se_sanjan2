import React from 'react';
import { Container, Typography } from '@mui/material';

const BrowseCoupons: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Browse Coupons (Coming Soon)</Typography>
    </Container>
  );
};

export default BrowseCoupons;
