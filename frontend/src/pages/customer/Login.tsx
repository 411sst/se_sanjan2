import React from 'react';
import { Container, Typography } from '@mui/material';

const CustomerLogin: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">Customer Login (OTP) - Coming Soon</Typography>
    </Container>
  );
};

export default CustomerLogin;
