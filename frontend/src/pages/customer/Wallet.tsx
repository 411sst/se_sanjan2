import React from 'react';
import { Container, Typography } from '@mui/material';

const CustomerWallet: React.FC = () => {
  return (
    <Container>
      <Typography variant="h4">My Wallet (Coming Soon)</Typography>
    </Container>
  );
};

export default CustomerWallet;
